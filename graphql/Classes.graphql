query Classes {
  classes {
    pageInfo {
      startCursor
      endCursor
    }
    totalCount
    edges {
      node {
        id
        title
        cover {
          url
        }
      }
    }
  }
}
